//Illegal expression with missing closing parenthesis
begin
  print "Good morning NJIT";
  //compute circle area and circumference
  r = 5.0;
  pi = .14;
  area = (pi * (r + r)/2;
  if (r) then print "Area: ", area;
end
  
